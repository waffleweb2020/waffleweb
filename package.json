{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
     "start": "export `cat env/.prod-env` && nuxt start",
    "ypm2": "pm2 start --name 'waffleweb' 'yarn start' "
  },
  "devDependencies": {
    "@types/aos": "^3.0.4",
    "nuxt": "^3.0.0",
    "sass": "^1.57.1",
    "sass-loader": "10"
  },
  "dependencies": {
    "@nuxtjs/svg": "^0.4.0",
    "axios": "^1.2.2",
    "nuxt-swiper": "^0.1.9"
  }
}
